apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-vector-db-chart.fullname" . }}
  labels:
    {{- include "new-vector-db-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-vector-db-chart.selectorLabels" . | nindent 4 }}
